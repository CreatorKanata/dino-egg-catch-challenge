"""src/robot: Robot integration package (LeKiwi base, dino-controller input, Drive Mode).

Hardware-independent logic (controller parsing, action mapping, drive state) lives in
stdlib-only modules so it can be unit-tested without a robot; hardware adapters are separate.
"""
