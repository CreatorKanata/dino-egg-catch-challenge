"""src/robot/recording: pick_egg demonstration recorder (`python -m robot.recording.record_pick_egg`).

Records LeRobot episodes from the catch pose with the fork's record_loop; kept apart from the
Manual Mode application, which must be closed while recording. See docs/lekiwi-app-development.md, section 6.
"""
